{createLocationInterface} = require './location'
{createHistoryInterface} = require './history'
{createWebsocketInterface} = require './websocket'
{createConsoleInterface} = require './console'

module.exports = {
  createHistoryInterface
  createLocationInterface
  createWebsocketInterface
  createConsoleInterface
}
